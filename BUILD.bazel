# gazelle:prefix github.com/sbueringer/kubectl-openstack-plugin

load("@bazel_gazelle//:def.bzl", "gazelle")

gazelle(
    name = "gazelle",
    prefix = "github.com/sbueringer/kubectl-openstack-plugin",
)
